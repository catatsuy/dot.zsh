.zsh
==================================

    git clone git://github.com/catatsuy/dot.zsh.git ~/.zsh
    git submodule init
    git submodule update

`.zsh.local` は自由にいじれる

`~/.zshenv` に

    export ZDOTDIR=$HOME/.zsh

が必要
